# Install Chromium on Ubuntu without Snap
Type commands below
```
  sudo add-apt-repository ppa:saiarcot895/chromium-beta
  sudo apt update
  sudo apt install chromium-browser
```

# Chromium on Lambda
```http://chromiumforlambda.com```
